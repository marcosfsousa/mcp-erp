# Normative statement register

Every normative statement — `MUST`, `MUST NOT`, `SHOULD`, `SHOULD NOT` — in a specification or RFC this exhibit claims to follow and **does not simply follow**: each deliberate departure, and each non-obvious reading. One row per statement.

Statements we follow plainly are not listed. They are proven instead, in [`attack-suite/scenarios.yaml`](attack-suite/scenarios.yaml), which carries its own citation and normative strength per row.

**Two row types, and they are not the same thing.** A **deviation** does something the statement forbids or omits something it asks for. An **interpretation** follows the statement, under a reading that is not the only available one. Flattening them would misrepresent both — an interpretation listed as a deviation overstates, and an unlisted reading of a `MUST` makes this register look curated rather than complete.

**This register points; it does not decide.** Every row is already decided in an ADR, and the *Reasoning* column is the pointer. A row with no ADR behind it is not yet decided and does not belong here yet.

**Add the row in the same commit as the ADR that creates it.** Reconstructing this set by re-reading the ADR trail at write-up time is how one gets missed.

**This standardises the shape [ADR-0005 §Deviations](adr/0005-the-authorization-server-is-a-dependency-not-a-deliverable.md) invented.** That section already did this job for one ticket, and the pattern was not carried forward — ADR-0003's amendment and ADR-0010 each recorded theirs in a different shape. New ADRs should add a row here rather than inventing a fourth. Keeping the prose in the ADR is correct; this is the index over it, not a replacement.

**Scope is normative statements only.** Departures from this project's own constraints — map constraint #11's floating-major exception for GitHub Actions, for instance — are not normative statements and stay where the constraint lives.

| # | Type | Spec and clause | Strength | What we do instead / how we read it | Why | Reasoning |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | Deviation | **RFC 8707 §2** — the authorization server "SHOULD audience-restrict issued access tokens to the resource(s) indicated by the `resource` parameter" | `SHOULD` | Keycloak does not honour `resource`, so it does not audience-restrict on that basis. Our clients send `resource` regardless, because the specification requires it independently of server support, and the **resource server's own audience check is the load-bearing control**. | Inherited from the chosen dependency rather than chosen by us — [keycloak#41526](https://github.com/keycloak/keycloak/issues/41526) closed *superseded, not implemented*; the replacement is milestoned after the build window. Survivable because the two specifications do not meet in our favour by accident: RFC 8707 §2 is only a `SHOULD` on the authorization server, while the MCP resource server is under a hard `MUST` to reject anything not issued for it. | [ADR-0005 §Deviations 1](adr/0005-the-authorization-server-is-a-dependency-not-a-deliverable.md) |
| 2 | Deviation | **RFC 8414 §2 and §3** — an issuer identifier is "a URL that uses the 'https' scheme", and the well-known path "MUST use the 'https' scheme". **RFC 9728 §1.2** — a resource identifier is "a URL that uses the https scheme". | `MUST` | Under Compose both use plain HTTP: issuer `http://keycloak:8081/realms/{realm}`, resource identifier `http://localhost:8080/mcp`. No carve-out for localhost, loopback or development exists in either document. | Offline reproducibility. No certificate authority issues a certificate for `keycloak:8081`, and a self-signed authority makes certificate trust a setup step for every third-party client — against map constraint #5 and ship line #8. **Not closed here.** The earlier claim that this was *"a property of the local harness, not of the design"* because a Cloud Run deployment would demonstrate the conformant form is **withdrawn**: that deployment was declined, and no environment this exhibit ships erases the deviation. The route that would close it is ADR-0005's option 6 — terminating TLS at a fixed hostname in Compose — as a **non-default opt-in profile**, declined for v1 on setup cost rather than on impossibility. | [ADR-0005 §Deviations 2](adr/0005-the-authorization-server-is-a-dependency-not-a-deliverable.md), [ADR-0011](adr/0011-it-runs-on-the-readers-machine-and-the-deviation-is-ours.md) |
| 3 | Deviation | **MCP Security Best Practices `2026-07-28` §State Handle Hijacking** — servers "SHOULD use secure, non-deterministic handles generated with secure random number generators. Avoid predictable or sequential identifiers that could be guessed by an attacker." | `SHOULD` | Requisition identifiers are sequential and legible (`req_0104`). The clause's companion `MUST NOT` — never treat possession of a handle as authentication — **is** honoured, and asserted twice: indistinguishable `not_found` on the read path, and a refused write that leaves state unmodified. | Following it would delete the proof of the half we do honour. ADR-0002 made indistinguishability the load-bearing control; unguessable identifiers mean the probe scenario can no longer *guess* a foreign identifier and must be handed one, turning a demonstrated defence into an asserted one. What is traded away is defence in depth, stated rather than hidden. | [ADR-0003 §Identifiers are sequential and legible](adr/0003-the-schema-is-the-policy-functions-argument-list.md), [ADR-0010](adr/0010-the-clause-decides-the-row-the-removal-decides-the-split.md) |
| 4 | **Interpretation** | **MCP Authorization Security Considerations `2026-07-28` §Access Token Privilege Restriction** — "MCP servers **MUST** validate access tokens before processing the request, ensuring the access token is issued specifically for the MCP server, and take all necessary steps to ensure no data is returned to unauthorized parties." (retrieved 2026-08-12) | `MUST` | We read *before processing* as **"do not act on the request"**, not **"do not parse it"**. Header and body agreement (gate step 2) is therefore proven *before* the token check (gate step 4). **The statement is followed** — nothing is acted on, and no data returned, before the token is validated. | The `server/discover` exemption has to know which method a request is, and the method arrives in a **caller-controlled header**. Granting the exemption on that header before proving header and body agree *is* an authentication bypass: send `Mcp-Method: server/discover` with `tools/call` in the body and the token check never runs. Ordering makes the attack structurally impossible rather than defended against. Parsing is unavoidable regardless, since `-32020` is itself a mandated response to an unauthenticated caller. **Listed because it is the judgement call most likely to be wrong**, and the one a hostile reader would come here to find. | [ADR-0006 §The gate order is a security property, not a style choice](adr/0006-fail-closed-in-a-fixed-order.md) |
| 5 | **Interpretation** | **MCP `2026-07-28` [basic/index §Statelessness](https://modelcontextprotocol.io/specification/2026-07-28/basic/index)** — "Servers **MUST NOT** rely on prior requests over the same connection to establish context (e.g., capabilities, protocol version, client identity). Every request supplies this metadata in its `_meta` field." (retrieved 2026-08-06) | `MUST NOT` | We read *relying on prior requests* as relying on their **state**, not as sharing a process with a leg that performs a handshake. **The statement is followed on both legs**: modern requests carry their metadata per request in `_meta`, and `stateless_http=True` gives legacy callers throwaway per-request sessions with no `Mcp-Session-Id` issued and nothing remembered between requests. | The official `mcp` 2.0.0 package serves both protocol eras from one endpoint and offers no switch to disable the legacy one, so the handshake is **inherited rather than built** — and a server that answers a `2025-11-25` `initialize` at all is not obviously the stateless server this clause describes, whatever it remembers. Right reading, still a reading. **Listed because a hostile reader of a stateless-protocol exhibit comes here first.** ADR-0008 conceded the point — *"sessions re-enter through a door we do not control"* — and sent it to the write-up because this register did not exist yet; it landed 2026-08-11 and the register was created 2026-08-12. | [ADR-0008 §The ticket's premise was inverted by the package that ships the protocol](adr/0008-the-run-is-over-the-wire-and-the-token-is-the-only-seam.md), [ADR-0009](adr/0009-not-built-is-not-unreachable.md), [ADR-0011](adr/0011-it-runs-on-the-readers-machine-and-the-deviation-is-ours.md) |
